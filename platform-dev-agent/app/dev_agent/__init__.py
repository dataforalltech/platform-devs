"""Single package root for the dev agent (Modo B engine).

The walking skeleton wires: runbook DAG -> PlanBuilder -> (executor, out of
scope here) -> GatewayToolClient. Personas/executor/approval/leader are NOT
part of this skeleton.
"""
