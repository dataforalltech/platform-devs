"""Domínio *product-manager* do devteam-mcp (persona Product Manager, consolidado).

Sub-pacote plugável do server AGREGADOR: expõe ``plugin.register()`` (catálogo de tools
+ dispatch + ensure_schema) sobre a ``ProductManagerStore`` (tenant-scoped, dual-db).
"""
