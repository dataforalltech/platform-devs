"""Legacy path retained only for non-destructive workspace compatibility.

The executable implementation resolves from ``mcp/project_product_mcp`` through
``project_product_mcp.__path__``. Persistence belongs exclusively to the API.
"""
