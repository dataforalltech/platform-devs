apiVersion: catalog.platform.dev/v1
kind: Operation
metadata:
  name: portfolio/project_create
  uid: portfolio.project_create
  domain: portfolio
  title: project create
  description: Create a project under a product while retaining only opaque external
    service references.
  owner: platform-engineering
  tags:
  - portfolio
  - project
  - write
  lifecycle: stable
  version: 1.0.0
spec:
  domain: portfolio
  capability: project-product-registry
  resource:
    type: project
    parent: null
    namespace: null
  operation: create
  authz: write
  risk:
    effects:
    - write
    requires: []
    blast_radius: tenant
    default_level: medium
    approval_required: none
  contract:
    inputs: {}
    outputs: {}
    preconditions:
    - trusted tenant context
    postconditions:
    - tenant-scoped audit record
    execution:
      idempotent: true
      side_effects: true
      is_async: false
      streaming: false
      timeout_s: 60
      retry_policy: idempotency-key
relations:
- verb: acts-on
  target: resource:project
