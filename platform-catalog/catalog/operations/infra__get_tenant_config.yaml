apiVersion: catalog.platform.dev/v1
kind: Operation
metadata:
  name: infra/get_tenant_config
  uid: infra.get_tenant_config
  domain: infra
  title: get tenant config
  description: Retorna variáveis de configuração do tenant. O tenant_id é resolvido
    das claims do inner token (gateway); via stdio direto pode ser informado.
  owner: platform
  tags:
  - infra
  - configuration
  - read
  lifecycle: stable
  version: 1.0.0
spec:
  domain: infra
  capability: configuration
  resource:
    type: tenant
    parent: null
    namespace: null
  operation: get
  authz: read
  risk:
    effects:
    - read
    requires: []
    blast_radius: none
    default_level: low
    approval_required: none
  contract:
    inputs:
      type: object
      properties:
        key_pattern:
          type: string
          description: 'Filtrar variáveis por nome (substring). Ex: ''DATABASE''.'
        limit:
          type: integer
          description: 'Máximo de variáveis retornadas. Padrão: 50.'
          default: 50
      additionalProperties: false
    outputs: {}
    preconditions: []
    postconditions: []
    execution:
      idempotent: true
      side_effects: false
      is_async: false
      streaming: false
      timeout_s: 60
      retry_policy: read-only
relations:
- verb: acts-on
  target: resource:tenant
