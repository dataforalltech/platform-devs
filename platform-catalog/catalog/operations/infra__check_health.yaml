apiVersion: catalog.platform.dev/v1
kind: Operation
metadata:
  name: infra/check_health
  uid: infra.check_health
  domain: infra
  title: check health
  description: RealizaHTTP GET no health endpoint de um servico registrado. Atualizalast_check_at
    e last_check_ok no registry.
  owner: platform
  tags:
  - infra
  - service-registry
  - write
  lifecycle: stable
  version: 1.0.0
spec:
  domain: infra
  capability: service-registry
  resource:
    type: service
    parent: null
    namespace: null
  operation: check
  authz: write
  risk:
    effects:
    - read
    requires: []
    blast_radius: service
    default_level: medium
    approval_required: none
  contract:
    inputs:
      type: object
      properties:
        name:
          type: string
          description: Nome do servico.
        timeout:
          type: number
          minimum: 0.1
          maximum: 30.0
          description: 'Timeout HTTP em segundos. Default: 3.0.'
          default: 3.0
      required:
      - name
      additionalProperties: false
    outputs: {}
    preconditions: []
    postconditions: []
    execution:
      idempotent: false
      side_effects: true
      is_async: false
      streaming: false
      timeout_s: 60
      retry_policy: none
relations:
- verb: acts-on
  target: resource:service
