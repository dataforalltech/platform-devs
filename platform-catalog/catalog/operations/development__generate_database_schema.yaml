apiVersion: catalog.platform.dev/v1
kind: Operation
metadata:
  name: development/generate_database_schema
  uid: development.generate_database_schema
  domain: development
  title: generate database schema
  description: Gera DDL SQL (CREATE TABLE) determinístico a partir de tables[{name,columns[{name,type,pk,fk}]}].
  owner: platform
  tags:
  - development
  - backend
  - write
  lifecycle: stable
  version: 1.0.0
spec:
  domain: development
  capability: backend
  resource:
    type: backend_artifact
    parent: null
    namespace: null
  operation: generate
  authz: write
  risk:
    effects:
    - generate
    requires: []
    blast_radius: service
    default_level: medium
    approval_required: none
  contract:
    inputs:
      type: object
      additionalProperties: false
      properties:
        tables:
          type: array
          description: Tabelas [{name, columns:[{name,type,pk?,fk?}]}].
      required:
      - tables
    outputs: {}
    preconditions: []
    postconditions: []
    execution:
      idempotent: false
      side_effects: true
      is_async: false
      streaming: false
      timeout_s: 60
      retry_policy: none
relations:
- verb: acts-on
  target: resource:backend_artifact
