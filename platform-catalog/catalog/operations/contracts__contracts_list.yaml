apiVersion: catalog.platform.dev/v1
kind: Operation
metadata:
  name: contracts/contracts_list
  uid: contracts.contracts_list
  domain: contracts
  title: list governed MCP contracts
  description: List canonical governed tool contracts with optional filters.
  owner: platform-engineering
  tags: [contracts, read]
  lifecycle: stable
  version: 1.0.0
spec:
  domain: contracts
  capability: contract-governance
  resource: {type: contract, parent: null, namespace: null}
  operation: list
  authz: read
  risk: {effects: [read], requires: [], blast_radius: none, default_level: low, approval_required: none}
  contract:
    inputs: {}
    outputs: {}
    preconditions: []
    postconditions: []
    execution: {idempotent: true, side_effects: false, is_async: false, streaming: false, timeout_s: 60, retry_policy: read-only}
relations: [{verb: acts-on, target: 'resource:contract'}]
