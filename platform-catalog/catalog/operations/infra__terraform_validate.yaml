apiVersion: catalog.platform.dev/v1
kind: Operation
metadata:
  name: infra/terraform_validate
  uid: infra.terraform_validate
  domain: infra
  title: terraform validate
  description: Roda `terraform validate -json` no diretório. Retorna diagnostics estruturados
    (erros + warnings). Read-only — não modifica state nem consulta provider remoto.
  owner: platform
  tags:
  - infra
  - provisioning
  - read
  lifecycle: stable
  version: 1.0.0
spec:
  domain: infra
  capability: provisioning
  resource:
    type: vm
    parent: null
    namespace: null
  operation: terraform
  authz: read
  risk:
    effects:
    - read
    requires:
    - terraform
    blast_radius: none
    default_level: low
    approval_required: none
  contract:
    inputs:
      type: object
      properties:
        path:
          type: string
          description: 'Diretório do módulo terraform. Default: INFRA_TERRAFORM_ROOT.'
      additionalProperties: false
    outputs: {}
    preconditions: []
    postconditions: []
    execution:
      idempotent: true
      side_effects: false
      is_async: false
      streaming: false
      timeout_s: 60
      retry_policy: read-only
relations:
- verb: acts-on
  target: resource:vm
