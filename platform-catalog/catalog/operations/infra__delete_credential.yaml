apiVersion: catalog.platform.dev/v1
kind: Operation
metadata:
  name: infra/delete_credential
  uid: infra.delete_credential
  domain: infra
  title: delete credential
  description: Remove (soft-delete) uma credencial do store central por namespace
    e chave; retorna se havia uma linha viva (idempotente).
  owner: platform
  tags:
  - infra
  - configuration
  - write
  lifecycle: stable
  version: 1.0.0
spec:
  domain: infra
  capability: configuration
  resource:
    type: credential
    parent: null
    namespace: null
  operation: delete
  authz: write
  risk:
    effects:
    - delete
    requires: []
    blast_radius: service
    default_level: high
    approval_required: N2
  contract:
    inputs:
      type: object
      properties:
        namespace:
          type: string
        key:
          type: string
      required:
      - namespace
      - key
      additionalProperties: false
    outputs: {}
    preconditions: []
    postconditions: []
    execution:
      idempotent: false
      side_effects: true
      is_async: false
      streaming: false
      timeout_s: 60
      retry_policy: none
relations:
- verb: acts-on
  target: resource:credential
