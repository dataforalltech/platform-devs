apiVersion: catalog.platform.dev/v1
kind: Operation
metadata:
  name: infra/terraform_fmt_check
  uid: infra.terraform_fmt_check
  domain: infra
  title: terraform fmt check
  description: Roda `terraform fmt -check -diff` (recursivo). Não modifica arquivos.
    Retorna lista de arquivos não-formatados + diff.
  owner: platform
  tags:
  - infra
  - provisioning
  - read
  lifecycle: stable
  version: 1.0.0
spec:
  domain: infra
  capability: provisioning
  resource:
    type: vm
    parent: null
    namespace: null
  operation: terraform
  authz: read
  risk:
    effects:
    - read
    requires:
    - terraform
    blast_radius: none
    default_level: low
    approval_required: none
  contract:
    inputs:
      type: object
      properties:
        path:
          type: string
        recursive:
          type: boolean
          default: true
      additionalProperties: false
    outputs: {}
    preconditions: []
    postconditions: []
    execution:
      idempotent: true
      side_effects: false
      is_async: false
      streaming: false
      timeout_s: 60
      retry_policy: read-only
relations:
- verb: acts-on
  target: resource:vm
