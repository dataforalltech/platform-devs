apiVersion: catalog.platform.dev/v1
kind: Operation
metadata:
  name: platform/defer_suggestion
  uid: platform.defer_suggestion
  domain: platform
  title: defer suggestion
  description: Difere uma sugestão pendente. actor obrigatório, reason opcional.
  owner: platform
  tags:
  - platform
  - session
  - write
  lifecycle: stable
  version: 1.0.0
spec:
  domain: platform
  capability: session
  resource:
    type: suggestion
    parent: null
    namespace: null
  operation: defer
  authz: write
  risk:
    effects:
    - write
    requires: []
    blast_radius: service
    default_level: medium
    approval_required: none
  contract:
    inputs:
      type: object
      additionalProperties: false
      properties:
        suggestion_id:
          type: integer
        actor:
          type: object
          additionalProperties: false
          description: Quem tomou a decisão (governança).
          properties:
            type:
              type: string
              enum:
              - human
              - agent
              - system
            id:
              type: string
              description: Email do humano, session_id do agente, ou nome do sistema.
          required:
          - type
          - id
        reason:
          type: string
      required:
      - suggestion_id
      - actor
    outputs: {}
    preconditions: []
    postconditions: []
    execution:
      idempotent: false
      side_effects: true
      is_async: false
      streaming: false
      timeout_s: 60
      retry_policy: none
relations:
- verb: acts-on
  target: resource:suggestion
