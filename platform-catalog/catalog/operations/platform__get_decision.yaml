apiVersion: catalog.platform.dev/v1
kind: Operation
metadata:
  name: platform/get_decision
  uid: platform.get_decision
  domain: platform
  title: get decision
  description: Retorna o registro completo de uma decisão pelo ID.
  owner: platform
  tags:
  - platform
  - session
  - read
  lifecycle: stable
  version: 1.0.0
spec:
  domain: platform
  capability: session
  resource:
    type: session
    parent: null
    namespace: null
  operation: get
  authz: read
  risk:
    effects:
    - read
    requires: []
    blast_radius: none
    default_level: low
    approval_required: none
  contract:
    inputs:
      type: object
      additionalProperties: false
      properties:
        decision_id:
          type: integer
      required:
      - decision_id
    outputs: {}
    preconditions: []
    postconditions: []
    execution:
      idempotent: true
      side_effects: false
      is_async: false
      streaming: false
      timeout_s: 60
      retry_policy: read-only
relations:
- verb: acts-on
  target: resource:session
