apiVersion: catalog.platform.dev/v1
kind: Operation
metadata:
  name: platform/get_suggestion
  uid: platform.get_suggestion
  domain: platform
  title: get suggestion
  description: Retorna o registro completo de uma sugestão pelo ID.
  owner: platform
  tags:
  - platform
  - session
  - read
  lifecycle: stable
  version: 1.0.0
spec:
  domain: platform
  capability: session
  resource:
    type: suggestion
    parent: null
    namespace: null
  operation: get
  authz: read
  risk:
    effects:
    - read
    requires: []
    blast_radius: none
    default_level: low
    approval_required: none
  contract:
    inputs:
      type: object
      additionalProperties: false
      properties:
        suggestion_id:
          type: integer
      required:
      - suggestion_id
    outputs: {}
    preconditions: []
    postconditions: []
    execution:
      idempotent: true
      side_effects: false
      is_async: false
      streaming: false
      timeout_s: 60
      retry_policy: read-only
relations:
- verb: acts-on
  target: resource:suggestion
