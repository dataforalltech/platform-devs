apiVersion: catalog.platform.dev/v1
kind: Operation
metadata:
  name: platform/start_task
  uid: platform.start_task
  domain: platform
  title: start task
  description: Marca uma tarefa como 'in_progress'. Só funciona a partir de 'pending'.
  owner: platform
  tags:
  - platform
  - session
  - write
  lifecycle: stable
  version: 1.0.0
spec:
  domain: platform
  capability: session
  resource:
    type: task
    parent: null
    namespace: null
  operation: start
  authz: write
  risk:
    effects:
    - write
    requires: []
    blast_radius: service
    default_level: medium
    approval_required: none
  contract:
    inputs:
      type: object
      additionalProperties: false
      properties:
        task_id:
          type: integer
          description: ID da tarefa
      required:
      - task_id
    outputs: {}
    preconditions: []
    postconditions: []
    execution:
      idempotent: false
      side_effects: true
      is_async: false
      streaming: false
      timeout_s: 60
      retry_policy: none
relations:
- verb: acts-on
  target: resource:task
