apiVersion: catalog.platform.dev/v1
kind: Operation
metadata:
  name: infra/service_status
  uid: infra.service_status
  domain: infra
  title: service status
  description: 'Retornadados do servico + health check em umaunicachamada. overall_status:
    healthy | unhealthy | unknown.'
  owner: platform
  tags:
  - infra
  - service-registry
  - read
  lifecycle: stable
  version: 1.0.0
spec:
  domain: infra
  capability: service-registry
  resource:
    type: service
    parent: null
    namespace: null
  operation: service
  authz: read
  risk:
    effects:
    - read
    requires: []
    blast_radius: none
    default_level: low
    approval_required: none
  contract:
    inputs:
      type: object
      properties:
        name:
          type: string
          description: Nome do servico.
        timeout:
          type: number
          minimum: 0.1
          maximum: 30.0
          description: 'Timeout HTTP em segundos. Default: 3.0.'
          default: 3.0
      required:
      - name
      additionalProperties: false
    outputs: {}
    preconditions: []
    postconditions: []
    execution:
      idempotent: true
      side_effects: false
      is_async: false
      streaming: false
      timeout_s: 60
      retry_policy: read-only
relations:
- verb: acts-on
  target: resource:service
