apiVersion: catalog.platform.dev/v1
kind: Operation
metadata:
  name: governance/get_compliance_checklist
  uid: governance.get_compliance_checklist
  domain: governance
  title: get compliance checklist
  description: Retorna checklist dinâmico para um repo/ambiente (preview sem persistir)
  owner: platform
  tags:
  - governance
  - audit
  - read
  lifecycle: stable
  version: 1.0.0
spec:
  domain: governance
  capability: audit
  resource:
    type: checklist
    parent: null
    namespace: null
  operation: get
  authz: read
  risk:
    effects:
    - read
    requires: []
    blast_radius: none
    default_level: low
    approval_required: none
  contract:
    inputs:
      type: object
      properties:
        service:
          type: string
          description: Nome do serviço
        repo:
          type: string
          description: Nome do repositório
        env:
          type: string
          description: 'Ambiente: dev, hml, prod'
          enum:
          - dev
          - hml
          - prod
      required:
      - service
      - repo
      - env
    outputs: {}
    preconditions: []
    postconditions: []
    execution:
      idempotent: true
      side_effects: false
      is_async: false
      streaming: false
      timeout_s: 60
      retry_policy: read-only
relations:
- verb: acts-on
  target: resource:checklist
