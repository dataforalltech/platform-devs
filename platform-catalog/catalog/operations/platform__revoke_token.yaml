apiVersion: catalog.platform.dev/v1
kind: Operation
metadata:
  name: platform/revoke_token
  uid: platform.revoke_token
  domain: platform
  title: revoke token
  description: Admin — Revoga um token ou todos os tokens de um user_id. Requer admin_token.
    O usuário perde acesso imediatamente.
  owner: platform
  tags:
  - platform
  - identity
  - write
  lifecycle: stable
  version: 1.0.0
spec:
  domain: platform
  capability: identity
  resource:
    type: token
    parent: null
    namespace: null
  operation: revoke
  authz: write
  risk:
    effects:
    - write
    requires: []
    blast_radius: service
    default_level: medium
    approval_required: none
  contract:
    inputs:
      type: object
      properties:
        admin_token:
          type: string
        identifier:
          type: string
          description: Token completo ou user_id a revogar.
      required:
      - admin_token
      - identifier
      additionalProperties: false
    outputs: {}
    preconditions: []
    postconditions: []
    execution:
      idempotent: false
      side_effects: true
      is_async: false
      streaming: false
      timeout_s: 60
      retry_policy: none
relations:
- verb: acts-on
  target: resource:token
