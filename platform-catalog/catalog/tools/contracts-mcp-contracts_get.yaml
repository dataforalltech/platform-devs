apiVersion: catalog.platform.dev/v1
kind: Tool
metadata:
  name: contracts-mcp/contracts_get
  uid: contracts-mcp:contracts_get
  domain: contracts
  title: contracts_get @ contracts-mcp
  description: Read one canonical governed tool contract.
  owner: platform-engineering
  tags: [contracts-mcp, contracts]
  lifecycle: stable
  version: 1.0.0
spec:
  operation_id: contracts.contracts_get
  provider_id: contracts-mcp
  provider_version: 1.0.0
  tool: contracts_get
  endpoint: streamable-http
  selection: {}
relations:
  - {verb: implements, target: contracts.contracts_get}
  - {verb: provided-by, target: contracts-mcp}
