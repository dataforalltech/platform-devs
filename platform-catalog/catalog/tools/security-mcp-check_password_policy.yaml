apiVersion: catalog.platform.dev/v1
kind: Tool
metadata:
  name: security-mcp/check_password_policy
  uid: security-mcp:check_password_policy
  domain: security
  title: check_password_policy @ security-mcp
  description: ''
  owner: platform
  tags:
  - security-mcp
  - security
  lifecycle: stable
  version: 1.0.0
spec:
  operation_id: security.check_password_policy
  provider_id: security-mcp
  provider_version: 1.0.0
  tool: check_password_policy
  endpoint: streamable-http
  selection: {}
relations:
- verb: implements
  target: security.check_password_policy
- verb: provided-by
  target: security-mcp
