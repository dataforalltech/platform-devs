# .env.infra.example — SÓ as senhas/tokens da infra (o resto já está no compose).
# Copie para .env (gitignored) e preencha. Gere valores fortes, ex.:
#   openssl rand -base64 32   |   python -c "import secrets;print(secrets.token_urlsafe(32))"

MYSQL_ROOT_PASSWORD=
POSTGRES_PASSWORD=
REDIS_PASSWORD=
VAULT_ROOT_TOKEN=
GRAFANA_ADMIN_PASSWORD=
