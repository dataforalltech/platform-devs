schema_version: 1
provider_id: devteam-mcp
name: config_read_env_file
description: Disabled because raw environment files may contain secret material.
status: disabled
input_schema:
  type: object
  additionalProperties: false
  properties:
    workspace_ref: {type: string}
  required: [workspace_ref]
output_schema: {type: object, properties: {artifact_ref: {type: string}}}
required_scope: config:environment:read
mutating: false
risk: {level: critical, effects: [secret_access], blast_radius: workspace, approval_required: N2}
secret_reference_only: true
