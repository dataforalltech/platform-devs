schema_version: 1
provider_id: devteam-mcp
name: config_get_credential
description: Disabled legacy operation that returned credential material directly to callers.
status: disabled
input_schema:
  type: object
  additionalProperties: false
  properties:
    credential_ref: {type: string}
  required: [credential_ref]
output_schema:
  type: object
  additionalProperties: false
  properties:
    credential_lease_ref: {type: string}
required_scope: config:credential:read
mutating: false
risk: {level: critical, effects: [secret_access], blast_radius: tenant, approval_required: N2}
secret_reference_only: true
