schema_version: 1
provider_id: connectors-mcp
name: alter_db_user_password
description: Disabled until the provider accepts an opaque credential reference instead of secret-bearing body data.
status: disabled
input_schema:
  type: object
  additionalProperties: false
  properties:
    connector_id: {type: integer}
    username: {type: string}
    credential_ref: {type: string}
  required: [connector_id, username, credential_ref]
output_schema: {type: object, properties: {status: {type: string}}}
required_scope: connectors:database:admin
mutating: true
risk: {level: critical, effects: [credential_rotation], blast_radius: tenant, approval_required: N2}
secret_reference_only: true
