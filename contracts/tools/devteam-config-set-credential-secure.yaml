schema_version: 1
provider_id: devteam-mcp
name: config_set_credential_secure
description: Disabled because the legacy operation depends on process TTY input.
status: disabled
input_schema:
  type: object
  additionalProperties: false
  properties:
    credential_ref: {type: string}
  required: [credential_ref]
output_schema: {type: object, properties: {status: {type: string}}}
required_scope: config:credential:write
mutating: true
risk: {level: critical, effects: [secret_write], blast_radius: tenant, approval_required: N2}
secret_reference_only: true
